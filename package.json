{
  "name": "better-jira-dots",
  "version": "1.3.1",
  "description": "Browser extension that customizes Jira's column-aging dots: hide them on configurable columns and skip weekends in the count.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:icons": "node scripts/generate-icons.mjs",
    "build": "node scripts/build.mjs",
    "package": "npm run build:icons && npm run build",
    "lint": "node scripts/lint.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "author": "Pim van Gurp",
  "repository": {
    "type": "git",
    "url": "https://github.com/pimvangurp/better-jira-dots"
  }
}
